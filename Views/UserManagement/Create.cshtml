@model CompanyApi.DTOs.UserDto

@{
    ViewData["Title"] = "Create User";
    Layout = "_Layout";
}

<div class="p-6">
    <div class="max-w-2xl mx-auto">
        <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
            <div class="mb-6">
                <h1 class="text-2xl font-bold text-gray-900 mb-2">Create New User</h1>
                <p class="text-gray-600">Add a new user account to the system.</p>
            </div>

            <form asp-action="Create" method="post">
                <div class="mb-4">
                    <label asp-for="@Model.Username" class="block text-sm font-medium text-gray-700 mb-2">Username</label>
                    <input asp-for="@Model.Username" class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent" required />
                    <span asp-validation-for="@Model.Username" class="text-red-500 text-sm mt-1"></span>
                </div>

                <div class="mb-4">
                    <label asp-for="@Model.Email" class="block text-sm font-medium text-gray-700 mb-2">Email</label>
                    <input asp-for="@Model.Email" type="email" class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent" required />
                    <span asp-validation-for="@Model.Email" class="text-red-500 text-sm mt-1"></span>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-4">
                    <div>
                        <label asp-for="@Model.FirstName" class="block text-sm font-medium text-gray-700 mb-2">First Name</label>
                        <input asp-for="@Model.FirstName" class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent" required />
                        <span asp-validation-for="@Model.FirstName" class="text-red-500 text-sm mt-1"></span>
                    </div>

                    <div>
                        <label asp-for="@Model.LastName" class="block text-sm font-medium text-gray-700 mb-2">Last Name</label>
                        <input asp-for="@Model.LastName" class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent" required />
                        <span asp-validation-for="@Model.LastName" class="text-red-500 text-sm mt-1"></span>
                    </div>
                </div>

                <div class="mb-4">
                    <label asp-for="@Model.PhoneNumber" class="block text-sm font-medium text-gray-700 mb-2">Phone Number</label>
                    <input asp-for="@Model.PhoneNumber" class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent" />
                    <span asp-validation-for="@Model.PhoneNumber" class="text-red-500 text-sm mt-1"></span>
                </div>

                <div class="mb-4">
                    <div class="flex items-center">
                        <input asp-for="@Model.IsActive" type="checkbox" class="h-4 w-4 text-primary focus:ring-primary border-gray-300 rounded" />
                        <label asp-for="@Model.IsActive" class="ml-2 block text-sm text-gray-900">Active</label>
                    </div>
                    <span asp-validation-for="@Model.IsActive" class="text-red-500 text-sm mt-1"></span>
                </div>

                <div class="mb-4">
                    <div class="flex items-center">
                        <input asp-for="@Model.IsAdmin" type="checkbox" class="h-4 w-4 text-primary focus:ring-primary border-gray-300 rounded" />
                        <label asp-for="@Model.IsAdmin" class="ml-2 block text-sm text-gray-900">Administrator</label>
                    </div>
                    <span asp-validation-for="@Model.IsAdmin" class="text-red-500 text-sm mt-1"></span>
                </div>

                <div class="flex justify-between items-center pt-6">
                    <a href="@Url.Action("Index", "UserManagement")" class="px-4 py-2 text-gray-700 border border-gray-300 rounded-lg hover:bg-gray-50 transition-colors">
                        <i class="fas fa-arrow-left mr-2"></i>Back to List
                    </a>
                    <button type="submit" class="px-6 py-2 bg-primary text-white rounded-lg hover:bg-blue-700 transition-colors">
                        <i class="fas fa-save mr-2"></i>Create User
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
